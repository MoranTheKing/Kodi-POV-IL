# בדיקת התקנה בטלפון

קובץ הבדיקה נמצא כאן:

`dist/Kodi-POV-IL-FENtastic-test-0.1.116.zip`

## מה לבדוק אחרי התקנה

1. Kodi עולה ישר עם `FENtastic`.
2. אפשר לפתוח את `POV` מהמסך הראשי.
3. כפתור חיבור שירותים פותח את מסך השירותים של `POV`.
4. `Real Debrid` ו־`TorBox` מופיעים כמופעלים בהגדרות של `POV`.
5. כפתור סטטוס `TorBox` נפתח בלי שגיאה.
6. כפתור סטטוס `Real Debrid` נפתח בלי שגיאה.
7. Widgets של סרטים וסדרות מציגים תוכן מ־`POV`.
8. כפתור חיפוש בסקין פותח חיפוש של `POV`.
9. בזמן ניגון, כפתורי בחירת מקור/פרק הבא עובדים מול `POV`.
10. דרך כפתור הכיבוי אפשר להחליף סקין חזרה ל־`Estuary`.

## הערות

- זו חבילת בדיקה ראשונה, לא release סופי.
- אם Kodi נפתח על `Estuary`, צריך לבדוק האם Android התעלם מ־`guisettings.xml` בזמן ההתקנה או אם הסקין לא הופעל במסד הנתונים.
- אם כפתור מסוים נפתח למסך ריק, צריך לרשום את שם הכפתור ואת המסך שבו זה קרה.
## Easier install path

Install the wizard zip in Kodi:

`dist/plugin.program.kodipovilwizard-latest.zip`

Then open:

`Add-ons -> Program add-ons -> Kodi POV IL Wizard -> Builds -> Kodi POV IL - FENtastic`

Choose a fresh install if you want this test build to replace the old POV build cleanly.

Direct GitHub download:

`https://github.com/MoranTheKing/Kodi-POV-IL/raw/main/dist/plugin.program.kodipovilwizard-latest.zip`

Kodi file source after GitHub Pages deploys:

`https://morantheking.github.io/Kodi-POV-IL/wizard/`

Path in Kodi:

`Install from zip file -> Kodi POV IL -> wizard -> plugin.program.kodipovilwizard-latest.zip`
