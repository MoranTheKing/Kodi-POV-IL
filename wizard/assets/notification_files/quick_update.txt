75
|||
תיקון progress missing במסלול של DarkSubs hook
דיווחת: לא ראית notification של התקדמות כשבחרת כתוביות באנגלית מ-DarkSubs וה-AI תרגם. צודק לחלוטין — היה באג שעוד לא תפסתי.

**שורש הבעיה:** ל-`_handle_download` (המסלול שבו המשתמש בוחר ב-AI Subs כשירות ישירות) **כן** הייתה DialogProgressBG עם עדכון לכל chunk. אבל ל-`_handle_translate_file` (המסלול שאליו DarkSubs hook קופץ דרך RunScript) — **לא היה** progress UI בכלל. הקריאה ל-`translate.resolve(link, info)` הייתה בלי `progress_cb`, אז שום מסלול עדכון UI לא קיים. התרגום פעל בשקט מתחילתו ועד סופו.

**התיקון:** הוספתי DialogProgressBG ל-`_handle_translate_file` שמטפל ב-3 המקרים:
- **התחלה**: יוצר dialog "Kodi POV IL - AI Subtitles"
- **תוך כדי**: report callback מתעדכן עם כל chunk שמסתיים → "מתרגם chunk 5/20..." בפינה הימנית-תחתונה של Kodi
- **סיום/קריסה**: `finally` block סוגר את ה-dialog נקי

אם יצירת ה-dialog עצמה נכשלת (נדיר) — התרגום ממשיך בשקט כמו ב-v0.2.39 (אין regression).

**מה כבר היה ועכשיו נוסף:**
- Toast "AI: מתחיל תרגום..." — כבר היה
- Progress bar למטה "מתרגם chunk X/Y..." — **חדש** במסלול DarkSubs (היה רק במסלול AI Subs ישיר)
- Toast "AI: תרגום הסתיים בהצלחה" — כבר היה

עכשיו לא משנה אם המשתמש בוחר דרך AI Subs ישירות או דרך DarkSubs — הוא יראה את אותו progress bar.

quick_update + restart.
