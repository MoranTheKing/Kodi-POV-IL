114
|||
תיקון כפתורי "הוסף לרשימות שלי" — TMDB / Trakt / POV
**הבעיה שדיווחת:** מוסיף דרך הכפתורים "ניהול רשימות" בעמוד סרטים/סדרות → מקבל "נוסף" → אבל באריחי "הסרטים שלי / הסדרות שלי" במרכז מסך הבית הרשימה ריקה. בדקת באתרי TMDB ו-Trakt — הפריטים **כן** נוספו שם.

**שתי בעיות שונות, שתיהן תוקנו ב-AI subs 0.2.68:**

**1. האריחים נעלמו ממסך הבית של FENtastic** (אחרי שהחלפת ל-AF3 וחזרת):
ה-wizard מחליף את `userdata/favourites.xml` ב-seed שמכיל **רק 11 אריחי שירותים** במקום 32 (חסרים 6 האריחים האישיים). תוקן עם patcher חדש `favourites_personal_tiles_patcher` שמזהה איזה מ-6 האריחים האישיים חסר ומשחזר אותם מ-fixture מצורף. רץ פעם אחת והשתחזרו.

**2. הקריאה מ-POV מציגה "ריק" למרות שעל השרת יש פריטים:**
ה-`cache_object` של POV שומר תוצאה ריקה ל-24 שעות. אם הקריאה ל-API נכשלה פעם אחת ארעי (רשת/auth/שרת), הרשימה הריקה נשמרה במטמון ל-24 שעות. גם אחרי שאתה מוסיף בהצלחה, ה-tile מציג את הריק הישן. תוקן עם patcher חדש `pov_cache_empty_patcher` שמוסיף `if not result: return result` לפני שמירת המטמון — תוצאות ריקות לא נשמרות יותר. בנוסף, בהפעלה הראשונה הוא מוחק one-shot את כל ה-tmdblist_* / trakt_* שתקועים ריקים אצלך עכשיו.

**זרימה צפויה:**
1. quick_update + restart.
2. בעלייה הראשונה: 6 האריחים יחזרו למסך הבית, וה-cache הריק יתנקה.
3. עכשיו לחץ על אריח "הסרטים שלי (TMDB)" — תראה את כל הסרטים שהוספת בימים האחרונים.

quick_update + restart.
